# Домашнее задание по теме "Системы мониторинга" Ячмень Марк Викторович

## Задание 1

Вас пригласили настроить мониторинг на проект. На онбординге вам рассказали, что проект представляет из себя платформу для вычислений с выдачей текстовых отчетов, которые сохраняются на диск. Взаимодействие с платформой осуществляется по протоколу http. Также вам отметили, что вычисления загружают ЦПУ. Какой минимальный набор метрик вы выведите в мониторинг и почему?

## Решение 1

Минимальный набор метрик:

1. ```CPU utilization / Load Average``` — позволяет контролировать загрузку процессора, поскольку основная работа платформы связана с вычислениями. Высокая загрузка CPU может приводить к увеличению времени выполнения задач.
2. ```RAM utilization``` — позволяет контролировать использование оперативной памяти и своевременно обнаруживать её нехватку.
3. ```Свободное место на диске``` — платформа сохраняет текстовые отчёты на диск, поэтому заполнение файловой системы может привести к невозможности сохранения новых отчётов.
4. ```Количество свободных inode``` — даже при наличии свободного дискового пространства создание новых файлов станет невозможным, если закончатся inode.
5. ```HTTP-коды ответов``` — количество и доля ответов 2xx, 4xx, 5xx позволяют оценивать доступность сервиса и количество ошибок.
6. ```Время ответа HTTP (latency)``` — позволяет контролировать скорость ответа платформы пользователям.
7. ```Количество HTTP-запросов (request rate)``` — позволяет оценивать текущую нагрузку на сервис.

Таким образом, данный набор позволяет контролировать как состояние инфраструктуры (CPU, RAM, диск, inode), так и состояние самого сервиса с точки зрения клиента (HTTP-коды, время ответа и количество запросов).

## Задание 2

Менеджер продукта посмотрев на ваши метрики сказал, что ему непонятно что такое RAM/inodes/CPUla. Также он сказал, что хочет понимать, насколько мы выполняем свои обязанности перед клиентами и какое качество обслуживания. Что вы можете ему предложить?

## Решение 2

Для менеджера продукта технические метрики инфраструктуры стоит дополнить показателями, непосредственно характеризующими качество работы сервиса для пользователя.

Можно использовать следующие показатели:

1. ```Доступность сервиса``` — доля времени, в течение которого платформа доступна пользователям.
2. ```Доля успешных запросов``` — отношение успешно обработанных HTTP-запросов к общему количеству запросов.
3. ```Время ответа сервиса``` — например, доля запросов, обработанных быстрее заданного времени.
4. ```Время формирования отчёта``` — поскольку основная функция платформы заключается в выполнении вычислений и создании отчётов, этот показатель непосредственно отражает качество работы сервиса для клиента.

Для формализации требований можно использовать понятия:

1. ```SLI (Service Level Indicator)``` — фактически измеряемый показатель качества работы сервиса. Например, доступность за месяц составила 99,95%, а 97% запросов были обработаны быстрее 500 мс.
2. ```SLO (Service Level Objective)``` — целевое значение SLI. Например: доступность сервиса должна быть не менее 99,9% в месяц или 95% HTTP-запросов должны выполняться быстрее 500 мс.
3. ```SLA (Service Level Agreement)``` — соглашение об уровне предоставления сервиса между поставщиком и потребителем. Оно может содержать несколько SLO и определять последствия их нарушения.

Например, для данной платформы можно установить SLO: не менее 99,9% успешных запросов за месяц, а фактически измеренную долю успешных запросов использовать как SLI. На основе этого показателя можно контролировать выполнение принятого SLA.

## Задание 3

Вашей DevOps команде в этом году не выделили финансирование на построение системы сбора логов. Разработчики в свою очередь хотят видеть все ошибки, которые выдают их приложения. Какое решение вы можете предпринять в этой ситуации, чтобы разработчики получали ошибки приложения?

## Решение 3

Если возможности развернуть централизованную систему сбора и хранения логов нет, можно настроить отправку сообщений об ошибках непосредственно из приложения.

Например, приложение может при возникновении ошибки отправлять уведомление разработчикам:

 - по электронной почте;
 - в корпоративный мессенджер через webhook;
 - в систему управления задачами (issue tracker).

В уведомление следует включать как минимум время возникновения ошибки, название приложения или компонента, уровень ошибки, текст сообщения и при необходимости stack trace.

При этом локальные логи приложения можно продолжать сохранять на сервере с настроенной ротацией (logrotate), чтобы при необходимости была возможность провести более подробный анализ.

Такое решение не заменяет полноценную централизованную систему логирования, поскольку затрудняет поиск, корреляцию и анализ большого количества событий, но позволяет разработчикам оперативно получать информацию об ошибках без развёртывания отдельной инфраструктуры сбора логов.

## Задание 4

Вы, как опытный SRE, сделали мониторинг, куда вывели отображения выполнения SLA=99% по http кодам ответов. Вычисляете этот параметр по следующей формуле: summ_2xx_requests/summ_all_requests. Данный параметр не поднимается выше 70%, но при этом в вашей системе нет кодов ответа 5xx и 4xx. Где у вас ошибка?

## Решение 4

Формула учитывает в числителе только ответы класса ```2xx```, но отсутствие ответов ```4xx``` и ```5xx``` не означает, что все остальные запросы завершились с кодами ```2xx```.

HTTP имеет и другие классы кодов ответа:

 - ```1xx``` — информационные ответы;
 - ```2xx``` — успешные ответы;
 - ```3xx``` — перенаправления;
 - ```4xx``` — ошибки клиента;
 - ```5xx``` — ошибки сервера.

Следовательно, оставшиеся 30% запросов могли завершиться, например, ответами класса 3xx. Они входят в summ_all_requests, но не входят в summ_2xx_requests.

Например:

```
Всего запросов: 1000
2xx:             700
3xx:             300
4xx:               0
5xx:               0
```

Тогда:

```
700 / 1000 × 100% = 70%
```

Таким образом, для выяснения причины необходимо проверить распределение запросов по всем классам HTTP-кодов, в первую очередь ```1xx``` и ```3xx```, а не только наличие ошибок ```4xx``` и ```5xx```.

## Задание 5

Опишите основные плюсы и минусы pull и push систем мониторинга.

## Решение 5

```Push-модель``` предполагает, что источник метрик самостоятельно отправляет данные в систему мониторинга.

Преимущества:

 - можно управлять частотой и моментом отправки метрик со стороны источника;
 - удобно передавать метрики от краткоживущих задач, которые могут завершиться до того, как система мониторинга успеет их опросить;
 - источник может отправлять данные через NAT или firewall, если исходящие соединения разрешены;
 - можно распределить отправку метрик во времени, снизив риск одновременного опроса большого количества узлов.

Недостатки:

 - сложнее контролировать источники централизованно;
 - если источник перестал отправлять метрики, необходимо отличать его отказ от отсутствия новых данных;
 - на источнике должна быть настроена информация о том, куда отправлять метрики.

```Pull-модель``` предполагает, что система мониторинга самостоятельно периодически обращается к контролируемым узлам и забирает у них метрики.

Преимущества:

 - централизованный контроль сбора метрик;
 - проще определить доступность наблюдаемого узла: если его невозможно опросить, система мониторинга это обнаруживает;
 - параметры и периодичность опроса можно менять централизованно;
 - источнику метрик не обязательно знать адрес сервера мониторинга.

Недостатки:

 - система мониторинга должна иметь сетевой доступ к каждому опрашиваемому узлу;
 - возникают сложности с узлами, находящимися за NAT или firewall;
 - краткоживущий процесс может завершиться между двумя опросами, и его метрики не будут получены.

Таким образом, нельзя однозначно назвать одну модель лучше другой. Выбор зависит от архитектуры системы и характера наблюдаемых объектов. На практике также возможно совместное использование обоих подходов.

## Задание 6

Какие из ниже перечисленных систем относятся к push модели, а какие к pull? А может есть гибридные?

    - Prometheus 
    - TICK
    - Zabbix
    - VictoriaMetrics
    - Nagios

## Решение 6

Классификация систем мониторинга по модели сбора метрик:

| Система | Модель | Пояснение |
|---|---|---|
| **Prometheus** | Pull | Prometheus самостоятельно периодически обращается к HTTP endpoints (`/metrics`) наблюдаемых объектов и забирает метрики. Для отдельных сценариев возможен push через Pushgateway. |
| **TICK** | Push | В типичной схеме Telegraf собирает метрики и самостоятельно отправляет их в InfluxDB. |
| **Zabbix** | Push и Pull | Поддерживает оба подхода. Passive checks — Zabbix Server/Proxy запрашивает данные у агента (pull), Active checks — агент самостоятельно отправляет собранные данные серверу (push). |
| **VictoriaMetrics** | Push и Pull | Поддерживает приём метрик по различным push-протоколам, а компонент `vmagent` также способен самостоятельно собирать (scrape) метрики с Prometheus-совместимых targets. |
| **Nagios** | Pull | В классической схеме сервер Nagios инициирует проверки контролируемых узлов и сервисов. Возможны также passive checks, при которых результаты проверок передаются в Nagios извне. |

Таким образом, некоторые системы нельзя строго отнести только к одной модели. Prometheus и классический Nagios преимущественно используют pull-подход, TICK — push-подход, а Zabbix и VictoriaMetrics позволяют использовать обе модели в зависимости от конфигурации.


## Задание 7

Склонируйте себе [репозиторий](https://github.com/influxdata/sandbox/tree/master) и запустите TICK-стэк, 
используя технологии docker и docker-compose.

В виде решения на это упражнение приведите скриншот веб-интерфейса ПО chronograf (`http://localhost:8888`). 

P.S.: если при запуске некоторые контейнеры будут падать с ошибкой - проставьте им режим `Z`, например
`./data:/var/lib:Z`

## Решение 7

Для выполнения задания выполним следующие действия.

Подготовим виртуальную машину с помощью Vagrant. В директории с Vagrant проектом создадим Vagrant файл следующего содержания:

```
Vagrant.configure("2") do |config|
  config.vm.box = "ubuntu/jammy64"

  config.vm.define "monitoring" do |monitoring|
    monitoring.vm.hostname = "monitoring"

    monitoring.vm.network "private_network", ip: "192.168.56.30"

    # Доступ к Chronograf с Windows
    monitoring.vm.network "forwarded_port",
      guest: 8888,
      host: 8888,
      auto_correct: true

    monitoring.vm.provider "virtualbox" do |vb|
      vb.name = "monitoring-systems-lab"
      vb.memory = 4096
      vb.cpus = 2
    end
  end
end
```

Запустим виртуальную машину ```monitoring-systems-lab```:

```
vagrant up
```
![img](img/image1.png)

Подключимся к созданной виртуальной машине и установим несколько базовых пакетов, необходимых для выполнения домашней работы:

```
vafrant ssh
sudo apt install -y ca-certificates curl git
```

![img](img/image2.png)

Добавим официальный репозиторий Docker.
Создадим каталог для ключей:

```
sudo install -m 0755 -d /etc/apt/keyrings
```

Скачаем GPG-ключ Docker:

```
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
```

Изменим права на файл ключа:

```
sudo chmod a+r /etc/apt/keyrings/docker.gpg
```

Добавим репозиторий Docker:

```
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

Устанавим Docker:

```
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

Проверим установку:

```
sudo systemctl status docker --no-pager
```

![img](img/image3.png)

Настроим работу с Docker без ```sudo```:

```
sudo usermod -aG docker vagrant
```

Проверим работу:

```
groups
docker run --rm hello-world
```

![img](img/image4.png)

Склонируем на виртуальную машину репозиторий из задания:

```
git clone https://github.com/influxdata/sandbox.git
```

![img](img/image5.png)


Так как скрипт ```./sandbox up``` рассчитан на старую отдельную команду docker-compose, а в системе установлен современный Docker Compose Plugin, запустим стек командой:

```
source .env-latest
docker compose up -d --build
```

Загрузка .env-latest потребовалась для установки переменной TYPE=latest, используемой при выборе Dockerfile для компонентов стека.

При первом запуске контейнеры Kapacitor и Chronograf завершались с ошибкой ```permission denied``` при записи в примонтированные каталоги. 
В задании для подобных проблем предлагается использовать режим :Z для Docker volumes. 
В используемой Ubuntu проблема была связана с правами доступа к каталогам и была устранена изменением разрешений:

```
sudo chmod -R 777 kapacitor/data
sudo chmod -R 777 chronograf/data
```

После изменения прав контейнеры Kapacitor и Chronograf успешно запустились.

Проверим текущее состояние всех контейнеров:

```
docker compose ps -a
```

![img](img/image6.png)

В браузере перейдём по адресу ```http://localhost:8888``` для того чтобы открыть веб-интерфейса ПО chronograf:

![img](img/image7.png)


## Задание 8

Перейдите в веб-интерфейс Chronograf (http://localhost:8888) и откройте вкладку Data explorer.
        
    - Нажмите на кнопку Add a query
    - Изучите вывод интерфейса и выберите БД telegraf.autogen
    - В `measurments` выберите cpu->host->telegraf-getting-started, а в `fields` выберите usage_system. Внизу появится график утилизации cpu.
    - Вверху вы можете увидеть запрос, аналогичный SQL-синтаксису. Поэкспериментируйте с запросом, попробуйте изменить группировку и интервал наблюдений.

Для выполнения задания приведите скриншот с отображением метрик утилизации cpu из веб-интерфейса.

## Решение 8

В веб-интерфейсе Chronograf перейдём в раздел Data Explorer:

![img](img/image8.png)

Теперь выберем базу данных ```telegraf.autogen```, ```measurement cpu```, хост ```telegraf-getting-started``` и поле ```usage_system```. 
В результате увидим сформированый InfluxQL-запрос и получим график утилизации CPU:

![img](img/image9.png)


## Задание 9

Изучите список [telegraf inputs](https://github.com/influxdata/telegraf/tree/master/plugins/inputs). 
Добавьте в конфигурацию telegraf следующий плагин - [docker](https://github.com/influxdata/telegraf/tree/master/plugins/inputs/docker):
```
[[inputs.docker]]
  endpoint = "unix:///var/run/docker.sock"
```

Дополнительно вам может потребоваться донастройка контейнера telegraf в `docker-compose.yml` дополнительного volume и 
режима privileged:
```
  telegraf:
    image: telegraf:1.4.0
    privileged: true
    volumes:
      - ./etc/telegraf.conf:/etc/telegraf/telegraf.conf:Z
      - /var/run/docker.sock:/var/run/docker.sock:Z
    links:
      - influxdb
    ports:
      - "8092:8092/udp"
      - "8094:8094"
      - "8125:8125/udp"
```

После настройке перезапустите telegraf, обновите веб интерфейс и приведите скриншотом список `measurments` в 
веб-интерфейсе базы telegraf.autogen . Там должны появиться метрики, связанные с docker.

Факультативно можете изучить какие метрики собирает telegraf после выполнения данного задания.

## Решение 9

В актуальной версии репозитория ```influxdata/sandbox``` плагин ```inputs.docker```, указанный в задании, уже присутствует в конфигурационном файле ```telegraf/telegraf.conf```. 
Также в ```docker-compose.yml``` уже настроено подключение ```/var/run/docker.sock``` к контейнеру Telegraf. Поэтому дополнительное добавление плагина и изменение ```docker-compose.yml``` не потребовались.

После запуска Telegraf в Chronograf автоматически появились Docker-метрики (```docker```, ```docker_container_cpu```, ```docker_container_mem```, ```docker_container_net```, ```docker_container_blkio```, ```docker_container_status```), что подтверждает корректную работу плагина inputs.docker:

![img](img/image10.png)

При запуске контейнера Telegraf была выявлена несовместимость конфигурации из репозитория с используемой актуальной версией Telegraf 1.39.3. Параметры ```container_names```, ```perdevice``` и ```total``` плагина ```inputs.docker``` больше не поддерживаются данной версией и приводили к завершению контейнера с ошибкой. 
Устаревшие параметры были удалены из ```telegraf/telegraf.conf```, после чего контейнер Telegraf успешно запустился и начал передавать Docker-метрики в InfluxDB.
